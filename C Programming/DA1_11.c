#include<stdio.h>

int main()
{
    // let x be the number of coins
    // therefore 0.25*x + 0.5*x + x = 700;
    // therefore x = 700/1.75;
    int x = 700/1.75;
    printf("%d",x);

    return 0;
}